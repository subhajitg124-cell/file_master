{
  "buildCommand": "cd artifacts/file-nova && npm install && npm run build",
  "outputDirectory": "artifacts/file-nova/dist/public",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
